# Patterns
## Arrays
    - subarry with largest sum
    - sort array in custom way
    - two pointer
    - prefix suffix
    - find duplicate no/ not duplicate no
    - matrix path traversal (left,right, up,down)
    - majority element(have freq more or equal to n/2)
    - largest consecutive sequence
    - largest subarray with sum equal to k
    - largest subarry without repeat no
    - trapping rain water
    - remove duplicate from sorted array
    - max consecutive onese
    - sliding window
## LinkedList
    - reverse linkedlist
    - middle of linkedist
    - merge two sorted linkedlist
    - add/remove/delete nth node
    - detect cycle
    - reverse a linkedlist
    - rotate
## Greedy(find min/max)
    -   fractional knapsack
    -   min coin required for Rs v

## Recurrsion and backtracking
    - all subset sum
    - kth permutation sequence
    - partition in palindrome with least palindrome count
    - print all permutation
    - rat in a maze(travel path)
## Binary Search
    - nth root of a number
    - search element in sorted array
    - kth element of two sorted array
    - case of max min (aggresive cow)
## Heap
    - max heap, mean heap (priority queue)
    - kth largest
    - top n frequent element
## Stack / Queue
    - implemnt stack.queue from array
    - check balanced paranthes balanced 
    - next greate element
    - inorder/reorder/postorder/left view /right view/level order
    - max path sum
    - symmetry binary tree
    - kth smallest element
    - kth largest lement
## Graph
    - DFS
    - BFS
    - cycle in a graph
    - Divide graph in bipartite

## String
    - reverse
    - longest palindrome
    - string to int (with condition)
    - longest common prefix
    - make string palindrome

## Dynamic Programming
    - longest cincreasing subsequence
    - longest common subsequence
    - 0/1 knapsack
    - max sum subsequence
    - matrix chain multiplication
    - min/max path
    - subset sum
    - rod cutting
    - egg dropping
## Trie
    - longest string with all prefix
    - power set(print all sub sequence)
    









## Algorithm
- sorting custom
- knapsack problem
- rabin karp(create hash of string then match
- knuth Morris algorithm
